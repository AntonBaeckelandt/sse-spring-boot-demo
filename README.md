# Server-Sent Events in Spring Boot

This repository contains 
- an example of how Server-Sent Events (SSE) can be implemented in Spring Boot (`sse-demo-server`)
- an example of how the events can be consumed (`sse-demo-client`)

Read about Server-Sent Events on my [blog](https://antonbaeckelandt.be/posts/6) (in Dutch).